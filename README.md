# directwa-marketplace
DirectWA Marketplace - South Africa
cd /home/workdir/artifacts/directwa-store

# Initialize git (if not already done)
git init

# Add all your files
git add .

# Create your first commit
git commit -m "Initial commit - DirectWA Marketplace"

# Connect to your new GitHub repository
# Replace YOUR_USERNAME with your actual GitHub username
git remote add origin https://github.com/YOUR_USERNAME/directwa-marketplace.git

# Push the code to GitHub
git branch -M main
git push -u origin main